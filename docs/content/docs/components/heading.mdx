---
title: Heading
description: "A semantic typography component that bridges the gap between design flexibility and HTML structure. It follows the Shadcn/ui philosophy: clean code, predictable behavior, and high customizability."
preview: true
links:
  api: /docs/components/heading#api-reference
---

<Callout type="info" title="Author's Note">
The Heading component was born out of a need for a more disciplined yet flexible approach to web typography. In many projects, I've seen semantic hierarchy sacrificed for visual styling, or vice versa. By decoupling the visual size from the HTML tag while enforcing sensible defaults like text-balance and tracking-tight, this component ensures that every headline in your app is not only SEO-friendly and accessible but also carries a premium, polished aesthetic without the manual overhead of repetitive Tailwind utility classes.
</Callout>

<ComponentTabs name="heading-demo" />

## Installation

### CLI

```package-install
npx shadcn@latest add @aloeui/heading
```

### Manual

<Steps>
  <Step>
    Install the following dependencies:

    ```package-install
    @radix-ui/react-slot class-variance-authority
    ```
  </Step>
  <Step>
    Copy and paste the following code into your project.

    <ComponentSource name="heading" />
  </Step>
</Steps>

## Examples

### Size

Choose the appropriate size (h1–h6).

<ComponentTabs name="heading-size-demo" />

### Weight

Control font weight with the `weight` prop.

<ComponentTabs name="heading-weight-demo" />

### Muted

Use `muted` for secondary headings.

<ComponentTabs name="heading-muted-demo" />

### Tracking & Leading

Adjust letter spacing and line height.

<ComponentTabs name="heading-tracking-demo" />

### Balance & Truncate

- `balance` (default: true) — enables `text-balance` for multi-line headings.
- `truncate` — truncates with ellipsis when overflow.

<ComponentTabs name="heading-balance-truncate-demo" />

### asChild

Use <code>asChild</code> to merge props with your child element (e.g. Link, motion.div).

<ComponentTabs name="heading-asChild-demo" />

## API Reference

### Heading

<AutoTypeTable path="types/docs/heading.ts" name="HeadingProps" />
